import { ThemeingDirective } from './themeing.directive';

describe('ThemeingDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeingDirective();
    expect(directive).toBeTruthy();
  });
});
