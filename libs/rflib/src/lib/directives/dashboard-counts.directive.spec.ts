import { DashboardCountsDirective } from './dashboard-counts.directive';

describe('DashboardCountsDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardCountsDirective();
    expect(directive).toBeTruthy();
  });
});
